name: Devnet Tests

on:
  push:
    branches: [main]
    paths:
      - "programs/agent-shield/src/**"
      - "tests/devnet-*.ts"
      - "tests/helpers/devnet-setup.ts"
      - ".github/workflows/devnet-test.yml"
      - "Anchor.toml"
  pull_request:
    branches: [main]
    paths:
      - "programs/agent-shield/src/**"
      - "tests/devnet-*.ts"
      - "tests/helpers/devnet-setup.ts"
      - ".github/workflows/devnet-test.yml"
      - "Anchor.toml"
  workflow_dispatch:

jobs:
  devnet-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile

      - name: Install Solana CLI
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      - name: Configure devnet wallet
        run: |
          mkdir -p "$HOME/.config/solana"
          echo '${{ secrets.DEVNET_WALLET_KEYPAIR }}' > "$HOME/.config/solana/id.json"
          solana config set --url https://api.devnet.solana.com --keypair "$HOME/.config/solana/id.json"

      - name: Fund wallet
        run: |
          echo "Current balance: $(solana balance)"
          echo "Requesting airdrops..."
          solana airdrop 2 || true
          sleep 6
          solana airdrop 2 || true
          sleep 6
          solana airdrop 2 || true
          sleep 6
          echo "Final balance: $(solana balance)"

      - name: Export wallet path
        run: |
          echo "ANCHOR_WALLET=$HOME/.config/solana/id.json" >> "$GITHUB_ENV"
          echo "ANCHOR_PROVIDER_URL=https://api.devnet.solana.com" >> "$GITHUB_ENV"

      - name: Run devnet tests
        run: |
          npx ts-mocha -p ./tsconfig.json -t 600000 \
            tests/devnet-smoke.ts \
            tests/devnet-sessions.ts \
            tests/devnet-fees.ts \
            tests/devnet-security.ts \
            tests/devnet-spending.ts \
            tests/devnet-transfers.ts \
            tests/devnet-positions.ts \
            tests/devnet-timelock.ts

      - name: Cleanup wallet
        if: always()
        run: rm -f "$HOME/.config/solana/id.json"
