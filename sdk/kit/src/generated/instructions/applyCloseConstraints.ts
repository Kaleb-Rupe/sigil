/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const APPLY_CLOSE_CONSTRAINTS_DISCRIMINATOR = new Uint8Array([
  133, 184, 235, 88, 53, 237, 43, 145,
]);

export function getApplyCloseConstraintsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLY_CLOSE_CONSTRAINTS_DISCRIMINATOR,
  );
}

export type ApplyCloseConstraintsInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountConstraints extends string | AccountMeta<string> = string,
  TAccountPendingCloseConstraints extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? WritableAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountConstraints extends string
        ? WritableAccount<TAccountConstraints>
        : TAccountConstraints,
      TAccountPendingCloseConstraints extends string
        ? WritableAccount<TAccountPendingCloseConstraints>
        : TAccountPendingCloseConstraints,
      ...TRemainingAccounts,
    ]
  >;

export type ApplyCloseConstraintsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApplyCloseConstraintsInstructionDataArgs = {};

export function getApplyCloseConstraintsInstructionDataEncoder(): FixedSizeEncoder<ApplyCloseConstraintsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: APPLY_CLOSE_CONSTRAINTS_DISCRIMINATOR,
    }),
  );
}

export function getApplyCloseConstraintsInstructionDataDecoder(): FixedSizeDecoder<ApplyCloseConstraintsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApplyCloseConstraintsInstructionDataCodec(): FixedSizeCodec<
  ApplyCloseConstraintsInstructionDataArgs,
  ApplyCloseConstraintsInstructionData
> {
  return combineCodec(
    getApplyCloseConstraintsInstructionDataEncoder(),
    getApplyCloseConstraintsInstructionDataDecoder(),
  );
}

export type ApplyCloseConstraintsAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountConstraints extends string = string,
  TAccountPendingCloseConstraints extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  constraints?: Address<TAccountConstraints>;
  pendingCloseConstraints?: Address<TAccountPendingCloseConstraints>;
};

export async function getApplyCloseConstraintsInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountConstraints extends string,
  TAccountPendingCloseConstraints extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApplyCloseConstraintsAsyncInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingCloseConstraints
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApplyCloseConstraintsInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingCloseConstraints
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: true },
    constraints: { value: input.constraints ?? null, isWritable: true },
    pendingCloseConstraints: {
      value: input.pendingCloseConstraints ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.constraints.value) {
    accounts.constraints.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([99, 111, 110, 115, 116, 114, 97, 105, 110, 116, 115]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.pendingCloseConstraints.value) {
    accounts.pendingCloseConstraints.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 101, 110, 100, 105, 110, 103, 95, 99, 108, 111, 115, 101, 95,
            99, 111, 110, 115, 116, 114, 97, 105, 110, 116, 115,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("constraints", accounts.constraints),
      getAccountMeta(
        "pendingCloseConstraints",
        accounts.pendingCloseConstraints,
      ),
    ],
    data: getApplyCloseConstraintsInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyCloseConstraintsInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingCloseConstraints
  >);
}

export type ApplyCloseConstraintsInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountConstraints extends string = string,
  TAccountPendingCloseConstraints extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  constraints: Address<TAccountConstraints>;
  pendingCloseConstraints: Address<TAccountPendingCloseConstraints>;
};

export function getApplyCloseConstraintsInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountConstraints extends string,
  TAccountPendingCloseConstraints extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApplyCloseConstraintsInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingCloseConstraints
  >,
  config?: { programAddress?: TProgramAddress },
): ApplyCloseConstraintsInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPolicy,
  TAccountConstraints,
  TAccountPendingCloseConstraints
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: true },
    constraints: { value: input.constraints ?? null, isWritable: true },
    pendingCloseConstraints: {
      value: input.pendingCloseConstraints ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("constraints", accounts.constraints),
      getAccountMeta(
        "pendingCloseConstraints",
        accounts.pendingCloseConstraints,
      ),
    ],
    data: getApplyCloseConstraintsInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyCloseConstraintsInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountConstraints,
    TAccountPendingCloseConstraints
  >);
}

export type ParsedApplyCloseConstraintsInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    policy: TAccountMetas[2];
    constraints: TAccountMetas[3];
    pendingCloseConstraints: TAccountMetas[4];
  };
  data: ApplyCloseConstraintsInstructionData;
};

export function parseApplyCloseConstraintsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApplyCloseConstraintsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
      constraints: getNextAccount(),
      pendingCloseConstraints: getNextAccount(),
    },
    data: getApplyCloseConstraintsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
