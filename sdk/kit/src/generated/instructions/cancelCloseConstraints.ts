/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const CANCEL_CLOSE_CONSTRAINTS_DISCRIMINATOR = new Uint8Array([
  150, 125, 186, 114, 40, 105, 237, 184,
]);

export function getCancelCloseConstraintsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_CLOSE_CONSTRAINTS_DISCRIMINATOR,
  );
}

export type CancelCloseConstraintsInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPendingCloseConstraints extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountPendingCloseConstraints extends string
        ? WritableAccount<TAccountPendingCloseConstraints>
        : TAccountPendingCloseConstraints,
      ...TRemainingAccounts,
    ]
  >;

export type CancelCloseConstraintsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelCloseConstraintsInstructionDataArgs = {};

export function getCancelCloseConstraintsInstructionDataEncoder(): FixedSizeEncoder<CancelCloseConstraintsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_CLOSE_CONSTRAINTS_DISCRIMINATOR,
    }),
  );
}

export function getCancelCloseConstraintsInstructionDataDecoder(): FixedSizeDecoder<CancelCloseConstraintsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelCloseConstraintsInstructionDataCodec(): FixedSizeCodec<
  CancelCloseConstraintsInstructionDataArgs,
  CancelCloseConstraintsInstructionData
> {
  return combineCodec(
    getCancelCloseConstraintsInstructionDataEncoder(),
    getCancelCloseConstraintsInstructionDataDecoder(),
  );
}

export type CancelCloseConstraintsAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPendingCloseConstraints extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  pendingCloseConstraints?: Address<TAccountPendingCloseConstraints>;
};

export async function getCancelCloseConstraintsInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPendingCloseConstraints extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: CancelCloseConstraintsAsyncInput<
    TAccountOwner,
    TAccountVault,
    TAccountPendingCloseConstraints
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelCloseConstraintsInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPendingCloseConstraints
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    pendingCloseConstraints: {
      value: input.pendingCloseConstraints ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.pendingCloseConstraints.value) {
    accounts.pendingCloseConstraints.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 101, 110, 100, 105, 110, 103, 95, 99, 108, 111, 115, 101, 95,
            99, 111, 110, 115, 116, 114, 97, 105, 110, 116, 115,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta(
        "pendingCloseConstraints",
        accounts.pendingCloseConstraints,
      ),
    ],
    data: getCancelCloseConstraintsInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelCloseConstraintsInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPendingCloseConstraints
  >);
}

export type CancelCloseConstraintsInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPendingCloseConstraints extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  pendingCloseConstraints: Address<TAccountPendingCloseConstraints>;
};

export function getCancelCloseConstraintsInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPendingCloseConstraints extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: CancelCloseConstraintsInput<
    TAccountOwner,
    TAccountVault,
    TAccountPendingCloseConstraints
  >,
  config?: { programAddress?: TProgramAddress },
): CancelCloseConstraintsInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPendingCloseConstraints
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    pendingCloseConstraints: {
      value: input.pendingCloseConstraints ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta(
        "pendingCloseConstraints",
        accounts.pendingCloseConstraints,
      ),
    ],
    data: getCancelCloseConstraintsInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelCloseConstraintsInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPendingCloseConstraints
  >);
}

export type ParsedCancelCloseConstraintsInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    pendingCloseConstraints: TAccountMetas[2];
  };
  data: CancelCloseConstraintsInstructionData;
};

export function parseCancelCloseConstraintsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelCloseConstraintsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      pendingCloseConstraints: getNextAccount(),
    },
    data: getCancelCloseConstraintsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
