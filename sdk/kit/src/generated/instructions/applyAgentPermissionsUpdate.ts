/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const APPLY_AGENT_PERMISSIONS_UPDATE_DISCRIMINATOR = new Uint8Array([
  234, 166, 205, 3, 28, 166, 221, 240,
]);

export function getApplyAgentPermissionsUpdateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLY_AGENT_PERMISSIONS_UPDATE_DISCRIMINATOR,
  );
}

export type ApplyAgentPermissionsUpdateInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountPendingAgentPerms extends string | AccountMeta<string> = string,
  TAccountAgentSpendOverlay extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? WritableAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountPendingAgentPerms extends string
        ? WritableAccount<TAccountPendingAgentPerms>
        : TAccountPendingAgentPerms,
      TAccountAgentSpendOverlay extends string
        ? WritableAccount<TAccountAgentSpendOverlay>
        : TAccountAgentSpendOverlay,
      ...TRemainingAccounts,
    ]
  >;

export type ApplyAgentPermissionsUpdateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApplyAgentPermissionsUpdateInstructionDataArgs = {};

export function getApplyAgentPermissionsUpdateInstructionDataEncoder(): FixedSizeEncoder<ApplyAgentPermissionsUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: APPLY_AGENT_PERMISSIONS_UPDATE_DISCRIMINATOR,
    }),
  );
}

export function getApplyAgentPermissionsUpdateInstructionDataDecoder(): FixedSizeDecoder<ApplyAgentPermissionsUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApplyAgentPermissionsUpdateInstructionDataCodec(): FixedSizeCodec<
  ApplyAgentPermissionsUpdateInstructionDataArgs,
  ApplyAgentPermissionsUpdateInstructionData
> {
  return combineCodec(
    getApplyAgentPermissionsUpdateInstructionDataEncoder(),
    getApplyAgentPermissionsUpdateInstructionDataDecoder(),
  );
}

export type ApplyAgentPermissionsUpdateAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPendingAgentPerms extends string = string,
  TAccountAgentSpendOverlay extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  pendingAgentPerms: Address<TAccountPendingAgentPerms>;
  /** Agent spend overlay — per-agent tracking slot. */
  agentSpendOverlay: Address<TAccountAgentSpendOverlay>;
};

export async function getApplyAgentPermissionsUpdateInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPendingAgentPerms extends string,
  TAccountAgentSpendOverlay extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApplyAgentPermissionsUpdateAsyncInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingAgentPerms,
    TAccountAgentSpendOverlay
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApplyAgentPermissionsUpdateInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingAgentPerms,
    TAccountAgentSpendOverlay
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
    pendingAgentPerms: {
      value: input.pendingAgentPerms ?? null,
      isWritable: true,
    },
    agentSpendOverlay: {
      value: input.agentSpendOverlay ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pendingAgentPerms", accounts.pendingAgentPerms),
      getAccountMeta("agentSpendOverlay", accounts.agentSpendOverlay),
    ],
    data: getApplyAgentPermissionsUpdateInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyAgentPermissionsUpdateInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingAgentPerms,
    TAccountAgentSpendOverlay
  >);
}

export type ApplyAgentPermissionsUpdateInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountPendingAgentPerms extends string = string,
  TAccountAgentSpendOverlay extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  pendingAgentPerms: Address<TAccountPendingAgentPerms>;
  /** Agent spend overlay — per-agent tracking slot. */
  agentSpendOverlay: Address<TAccountAgentSpendOverlay>;
};

export function getApplyAgentPermissionsUpdateInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountPendingAgentPerms extends string,
  TAccountAgentSpendOverlay extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: ApplyAgentPermissionsUpdateInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingAgentPerms,
    TAccountAgentSpendOverlay
  >,
  config?: { programAddress?: TProgramAddress },
): ApplyAgentPermissionsUpdateInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPolicy,
  TAccountPendingAgentPerms,
  TAccountAgentSpendOverlay
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
    pendingAgentPerms: {
      value: input.pendingAgentPerms ?? null,
      isWritable: true,
    },
    agentSpendOverlay: {
      value: input.agentSpendOverlay ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("pendingAgentPerms", accounts.pendingAgentPerms),
      getAccountMeta("agentSpendOverlay", accounts.agentSpendOverlay),
    ],
    data: getApplyAgentPermissionsUpdateInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyAgentPermissionsUpdateInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountPendingAgentPerms,
    TAccountAgentSpendOverlay
  >);
}

export type ParsedApplyAgentPermissionsUpdateInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    policy: TAccountMetas[2];
    pendingAgentPerms: TAccountMetas[3];
    /** Agent spend overlay — per-agent tracking slot. */
    agentSpendOverlay: TAccountMetas[4];
  };
  data: ApplyAgentPermissionsUpdateInstructionData;
};

export function parseApplyAgentPermissionsUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApplyAgentPermissionsUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
      pendingAgentPerms: getNextAccount(),
      agentSpendOverlay: getNextAccount(),
    },
    data: getApplyAgentPermissionsUpdateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
