/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PENDING_CLOSE_CONSTRAINTS_DISCRIMINATOR = new Uint8Array([
  128, 154, 58, 181, 85, 163, 243, 233,
]);

export function getPendingCloseConstraintsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PENDING_CLOSE_CONSTRAINTS_DISCRIMINATOR,
  );
}

export type PendingCloseConstraints = {
  discriminator: ReadonlyUint8Array;
  vault: Address;
  queuedAt: bigint;
  executesAt: bigint;
  bump: number;
};

export type PendingCloseConstraintsArgs = {
  vault: Address;
  queuedAt: number | bigint;
  executesAt: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link PendingCloseConstraintsArgs} account data. */
export function getPendingCloseConstraintsEncoder(): FixedSizeEncoder<PendingCloseConstraintsArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vault", getAddressEncoder()],
      ["queuedAt", getI64Encoder()],
      ["executesAt", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PENDING_CLOSE_CONSTRAINTS_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link PendingCloseConstraints} account data. */
export function getPendingCloseConstraintsDecoder(): FixedSizeDecoder<PendingCloseConstraints> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vault", getAddressDecoder()],
    ["queuedAt", getI64Decoder()],
    ["executesAt", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link PendingCloseConstraints} account data. */
export function getPendingCloseConstraintsCodec(): FixedSizeCodec<
  PendingCloseConstraintsArgs,
  PendingCloseConstraints
> {
  return combineCodec(
    getPendingCloseConstraintsEncoder(),
    getPendingCloseConstraintsDecoder(),
  );
}

export function decodePendingCloseConstraints<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PendingCloseConstraints, TAddress>;
export function decodePendingCloseConstraints<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PendingCloseConstraints, TAddress>;
export function decodePendingCloseConstraints<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<PendingCloseConstraints, TAddress>
  | MaybeAccount<PendingCloseConstraints, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPendingCloseConstraintsDecoder(),
  );
}

export async function fetchPendingCloseConstraints<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PendingCloseConstraints, TAddress>> {
  const maybeAccount = await fetchMaybePendingCloseConstraints(
    rpc,
    address,
    config,
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePendingCloseConstraints<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PendingCloseConstraints, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePendingCloseConstraints(maybeAccount);
}

export async function fetchAllPendingCloseConstraints(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PendingCloseConstraints>[]> {
  const maybeAccounts = await fetchAllMaybePendingCloseConstraints(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePendingCloseConstraints(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PendingCloseConstraints>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodePendingCloseConstraints(maybeAccount),
  );
}

export function getPendingCloseConstraintsSize(): number {
  return 57;
}
